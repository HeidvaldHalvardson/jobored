import React from 'react';
import {FilterLabelTitleStyled} from "./style";

const FilterLabelTitle = ({children}) => {
  return (
    <FilterLabelTitleStyled>{children}</FilterLabelTitleStyled>
  );
};

export default FilterLabelTitle;